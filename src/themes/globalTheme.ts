import {DefaultTheme} from 'styled-components';

const globalTheme: DefaultTheme = {
    borderRadius: '20px',
    boxShadow: '0px 0px 18px -6px rgba(0,0,0,0.75)',
    size: {
        header: {
            height: '100px'
        }
    },
    colors: {
        textWhite: '#fff',
        backgroundWhite: '#fff',
        backgroundGrey: 'rgb(45, 45, 45)'
    }
};

export {globalTheme};